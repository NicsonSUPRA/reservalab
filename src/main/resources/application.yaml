spring:
  application:
    name: reservalab
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:postgresql://localhost:5433/reservalab
    username: postgres
    password: postgres
